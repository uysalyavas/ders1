config:
  target: "http://httpbin.org/"
  phases:
    - duration: 1
      arrivalRate: 2
      name: Goit Performance Test 

scenarios:
  - name: Get a resource
    flow:
      - get:
          url: "{{target}}get"
